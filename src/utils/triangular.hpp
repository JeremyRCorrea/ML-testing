# include <cmath>

// A function that computes any triangular number
int triangular(int n) {

    // Compute the n-th triangular number
    int T_n = n*(n+1)/2;

    // Return result
    return T_n;
}